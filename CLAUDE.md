# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run build   # compile TypeScript to dist/
```

There are no test or lint scripts configured yet.

## Architecture

This is a TypeScript Node.js project in its initial scaffold stage. Source files live in `src/`, compiled output goes to `dist/`. The TypeScript config targets ES2016 with CommonJS modules and strict mode enabled.
